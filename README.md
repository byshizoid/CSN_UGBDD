# Карта перекрытий

Сайт для отображения карты с перекрытиями и фото местности.

## 🚀 Автоматическая синхронизация с GitHub

### Вариант 1: Автоматическая синхронизация при редактировании в Cursor

**Просто запустите скрипт один раз и оставьте его работать:**

1. **Убедитесь, что установлены:**
   - Node.js (https://nodejs.org/)
   - Git (https://git-scm.com/)

2. **Настройте git репозиторий** (если еще не сделано):
   ```bash
   git init
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git branch -M main
   ```

3. **Запустите скрипт автосинхронизации:**
   
   **Windows:**
   ```bash
   setup-auto-sync.bat
   npm run watch
   ```
   
   **Mac/Linux:**
   ```bash
   chmod +x setup-auto-sync.sh
   ./setup-auto-sync.sh
   npm run watch
   ```

4. **Оставьте терминал открытым** - теперь любые изменения в Cursor автоматически попадут в GitHub!

### Как это работает:

- ✏️ Вы редактируете код в Cursor (сохраняете файл Ctrl+S)
- 👀 Скрипт отслеживает изменения в `index.html`, `style.css`, `script.js`
- ⏱️ Через 5 секунд после последнего изменения автоматически создается коммит
- 🚀 Коммит автоматически пушится в GitHub
- ✅ Изменения сразу видны в GitHub репозитории

### Вариант 2: Через Git Hooks (автоматически при сохранении)

1. Запустите `setup-auto-sync.bat` (Windows) или `./setup-auto-sync.sh` (Mac/Linux)
2. Git hook установится автоматически
3. Теперь при любом сохранении файла в Cursor изменения автоматически коммитятся и пушатся

### Важно:

- ⚠️ Скрипт должен быть запущен в терминале (оставьте его работать)
- ✅ Убедитесь, что у вас настроен git и есть доступ к GitHub
- 🔄 Не нужно вручную делать `git add`, `git commit`, `git push` - все автоматически!

## 🌐 Открытие сайта через GitHub Pages

### Быстрая настройка:

1. **Запустите скрипт настройки:**
   ```bash
   setup-github-pages.bat
   ```
   Скрипт откроет ваш репозиторий на GitHub и покажет инструкцию.

2. **Или настройте вручную:**
   - Откройте ваш репозиторий: https://github.com/byshizoid/CSN_UGBDD
   - Нажмите **Settings** → **Pages**
   - В разделе **Source** выберите:
     - Branch: `master`
     - Folder: `/ (root)`
   - Нажмите **Save**

3. **Подождите 1-2 минуты** - GitHub опубликует ваш сайт

4. **Откройте сайт:**
   - Ваш сайт будет доступен по адресу: **https://byshizoid.github.io/CSN_UGBDD**

### После настройки:

- ✅ Сайт автоматически обновляется при каждом push в GitHub
- ✅ Доступен с любого устройства по ссылке GitHub Pages
- ✅ Изменения появляются на сайте через 1-2 минуты после push

---

## Использование сайта

1. Откройте сайт через GitHub Pages или `index.html` локально
2. Нажмите "🔐 Режим администратора" (пароль: `admin123`)
3. Загрузите карту и фото перекрытий
4. Все изменения автоматически сохраняются в GitHub через API сайта

