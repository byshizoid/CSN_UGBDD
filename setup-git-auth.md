# 🔐 Настройка аутентификации GitHub для Git

Для синхронизации файлов с GitHub нужна аутентификация. GitHub не принимает обычные пароли - нужен **Personal Access Token (PAT)**.

## 📝 Шаг 1: Создание Personal Access Token

1. Откройте GitHub: https://github.com
2. Зайдите в свой аккаунт
3. Перейдите в **Settings** (Настройки) → **Developer settings** (внизу слева)
4. Выберите **Personal access tokens** → **Tokens (classic)**
5. Нажмите **Generate new token** → **Generate new token (classic)**
6. Заполните форму:
   - **Note**: `Для синхронизации репозитория CSN_UGBDD` (любое название)
   - **Expiration**: Выберите срок действия (например, 90 дней или No expiration)
   - **Scopes**: Выберите **`repo`** (полный доступ к репозиториям)
7. Нажмите **Generate token**
8. **ВАЖНО**: Скопируйте токен сразу! Он больше не будет показан

## 🔧 Шаг 2: Настройка Git для использования токена

### Вариант 1: Через Git Credential Manager (Рекомендуется)

Токен будет сохранен в безопасном хранилище:

```bash
# Git попросит ввести токен при первом push
git push
```

Когда Git спросит:
- **Username**: ваш username на GitHub (например: `byshizoid`)
- **Password**: вставьте **Personal Access Token** (не пароль!)

### Вариант 2: Сохранить токен в Git config (постоянно)

```bash
# Сохранить токен для этого репозитория
git config credential.helper store

# Или для всех репозиториев
git config --global credential.helper store
```

При первом push введите токен, он сохранится.

### Вариант 3: Использовать токен в URL (не рекомендуется для безопасности)

```bash
git remote set-url origin https://ВАШ-ТОКЕН@github.com/byshizoid/CSN_UGBDD.git
```

⚠️ **Внимание**: Токен будет виден в истории команд!

## 🔄 Шаг 3: Синхронизация файлов

После настройки аутентификации:

```bash
# Добавить все файлы
git add .

# Создать коммит
git commit -m "Обновление файлов"

# Отправить в GitHub
git push
```

Или используйте скрипт:
```bash
sync-to-github.bat
```

## 🔍 Проверка настройки

Проверить текущую настройку remote:
```bash
git remote -v
```

Если remote настроен правильно, вы увидите:
```
origin  https://github.com/byshizoid/CSN_UGBDD.git (fetch)
origin  https://github.com/byshizoid/CSN_UGBDD.git (push)
```

## ⚠️ Важно

- **НЕ делитесь** токеном с другими
- **НЕ публикуйте** токен в коде или репозитории
- Если токен скомпрометирован, немедленно удалите его в GitHub Settings
- Токен можно использовать вместо пароля во всех Git операциях

## 🆘 Если что-то не работает

1. Проверьте, что токен имеет права `repo`
2. Убедитесь, что токен не истек
3. Проверьте правильность username и названия репозитория
4. Попробуйте создать новый токен

